#include "main.h"
/**
* _islower - a function that checks for lowercase character.
*Return: Always 0 (Success)
*/

int _islower(int c)
{
	if (n >= 65 && n <= 90)
	{
		return (0);
	} else if (n >= 97 && n <= 122)
	{
		return (1);
	}
}
